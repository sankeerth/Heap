#include "HeapFactory.h"
#include "log.h"
int main()
{
	/*
	HeapType type = Min;
	Heap* h = HeapFactory::create(type);	

	h->insertKey(3);
    h->insertKey(2);
    h->removeKey(1);
    h->insertKey(15);
    h->insertKey(5);
    h->insertKey(4);
    h->insertKey(45);
    h->insertKey(6);
    h->insertKey(7);
    h->print();
    LOG("");
    LOG(h->extractMinimum());
    LOG(h->getMinimum());
    LOG("");
    h->print();
    LOG("");
    h->decreaseKey(2, 1);
    LOG(h->getMinimum());
    h->print();

    delete h;
    */
    return 0;
}